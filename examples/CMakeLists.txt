if(BUILD_EXAMPLES OR HQ_BUILD_EXAMPLES)
  add_subdirectory(posix)
  message(STATUS "Examples: POSIX demo applications will be built")
else()
  message(STATUS "Examples: Skipped (enable with -DHQ_BUILD_EXAMPLES=ON)")
endif()