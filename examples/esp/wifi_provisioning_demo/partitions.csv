# Partition table for the Wi-Fi provisioning demo.
#
# The littlefs "storage" partition holds the saved Wi-Fi credential list
# (wifi_ap.json) written by the provisioning flow. Erasing this partition
# (or the whole flash) returns the device to the out-of-box state so the
# automatic fallback opens the portal again — see README.md for the
# erase-and-reprovision walkthrough.
# Name,      Type, SubType,   Offset,   Size,    Flags
nvs,         data, nvs,       0x9000,   0x4000,
phy_init,    data, phy,       0xd000,   0x1000,
factory,     app,  factory,   0x10000,  2M,
storage,     data, littlefs,  ,         256K,