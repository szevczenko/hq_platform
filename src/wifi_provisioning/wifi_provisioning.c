/*
 * Wi-Fi provisioning component scaffold.
 *
 * The component is compiled only when CONFIG_WIFI_HTTP_PROVISIONING is
 * enabled. This translation unit exists so the component always has at least
 * one source file. HTTP provisioning portal, captive DNS responder, and
 * Wi-Fi behavior are implemented by later tasks; nothing is added here yet.
 */

#include "hq_config.h"